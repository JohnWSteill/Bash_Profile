cd /w5home/$USER
export PATH=/usr/local/bin:/bin:/usr/bin:/usr/local/sbin:/usr/sbin
source /isitools/.bash_profile_python3.7.2
export PATH=/isitools/cactus-bin-v1.2.3/bin:$PATH
export PYTHONPATH=/isitools/cactus-bin-v1.2.3/lib:/isitools/cactus-bin-v1.2.3/bin:$PYTHONPATH

# create virtual env (need only run once to generate virtenv)
virtualenv -p python3.7 cactus_env_v1.2.3

# activate virtual env
source ./cactus_env_v1.2.3/bin/activate

# install/satisfy dependencies: (need only run these next 3 commands once to satisfy virtenv dependencies)
pip install -U setuptools pip
pip install -U -r /isitools/cactus-bin-v1.2.3/toil-requirement.txt
pip install -U /isitools/cactus-bin-v1.2.3/.

# RUNNING CACTUS:
# cd ~/cactus.test/examples/hal2assemblyHub_v1.2.3
#(cactus_env_v1.2.3) bash-4.2$ nohup cactus ./jobstore ./evolverMammals.txt ./evolverMammals.hal --realTimeLogging &
#(cactus_env_v1.2.3) bash-4.2$ hal2assemblyHub.py jobstore evolverMammals.hal evolverMammals_hub --lod --alignability --gcContent --hub evolverMammals --shortLabel evolverMammals |& tee hal2assemblyHub.out

